commands:
  - label: Build & test 
    prompt: |
      Run the compile/build & test suite. Fix every failing test and build error. Do not stop until the build compiles and test suite both pass. Do not change anything else.

pi:
  provider: openrouter
  model: deepseek/deepseek-v4-flash
  thinking: medium